# Agent-Team 8.0.14 release readiness

Version 8.0.14 is an **unpublished candidate**. It handles bd 1.2.2 expanded dependency issue projections while retaining strict dependency relation identities and types.

## Pending release evidence

The release workflow must still build the exact revision and complete its Linux, macOS, and Windows checks, package validation, archive and Windows-bundle canaries, rollback, provider verification, installed-update test, and revision-bound readiness gate. No publication, downloadable 8.0.14 assets, or post-publication installation result is claimed here.

Prospective assets are `agent-teamctl-8.0.14.zip`, `RELEASE.json`, `SBOM.cdx.json`, `SHA256SUMS`, and `agent-teamctl-8.0.14-windows-amd64.zip` with its `.sha256` sidecar. These names do not establish publication or availability. Until the workflow succeeds and the release is published, v8.0.13 remains the published package.
