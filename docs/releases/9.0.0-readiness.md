# Agent-Team v9.0.0 release readiness

This is a candidate evidence ledger, not a release claim. The package workflow
creates an artifact only; it does not tag, publish, push, or create a GitHub
Release. A later explicit release action remains gated on recorded native-host
and downloaded-package evidence.

| Evidence | Status | Record |
| --- | --- | --- |
| Candidate revision | Superseded | [Run 36106466674](https://github.com/thebpandey/agent-team/actions/runs/36106466674) passed at `ea749d9`; the reviewed cutover correction at `66f72f6` requires a new final candidate. |
| Archive path whitelist and SHA-256 | Verified for `ea749d9` | Artifact `10851326442` contained the expected 12 paths; downloaded ZIP SHA-256 was `f9282d448ef60d35371a77aac619779af7d03e8af1aa382d81641d41cb7b6594`. Recheck the final candidate. |
| Linux CI Unix installer test | Verified for `ea749d9` | `ubuntu-latest` passed in run `36106466674`. |
| Windows CI native PowerShell installer test | Verified for `ea749d9` | `windows-latest` passed in run `36106466674`. This is installer evidence, not a native Codex/Claude host canary. |
| Beads 1.2.2 download, checksum, and fixture | Verified for `ea749d9` | The pinned release asset, disposable init, readonly status, and bounded ready command passed in run `36106466674`. |
| Downloaded archive checksum and Linux installation | Verified for `ea749d9` | Downloaded artifact `10851326442` passed `sha256sum -c`; its unpacked installer installed identical skill trees into disposable Codex and Claude homes. |
| Downloaded archive checksum and Windows installation | Unverified | No published/downloaded candidate has been checked. |
| Linux native Codex canary | Unverified | CI and installer tests do not prove native host dispatch. |
| Linux native Claude canary | Unverified | CI and installer tests do not prove native host dispatch. |
| Windows native Codex canary | Unverified | No Windows native-host canary is recorded. |
| Windows native Claude canary | Unverified | No Windows native-host canary is recorded. |
| Quiescent v8 cutover and rollback smoke | Partial only | A disposable Linux dual-host v9 install/rollback preserved Git, Beads, and project files; an uncertain-work fixture stopped before mutation. No managed v8 uninstall or live-host cutover was run. |
| Optional Project Kickoff v9 bridge | Unverified | This is an interoperability report, not an Agent-Team standalone release gate. |

The standalone candidate must continue to work without Project Kickoff. Missing
Project Kickoff bridge evidence limits only that optional interoperability
claim; it must not be substituted for Beads, installer, archive-download, or
native-host evidence.

The v8 managed uninstaller covers every host in its manifest and discovers
default host homes even when explicit homes are supplied. The disposable smoke
therefore did not invoke it: doing so could inspect the live installation.
Before publishing, run the corrected candidate and perform the separately
gated live cutover with exact-byte recovery material for every manifest path.
