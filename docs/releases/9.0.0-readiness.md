# Agent-Team v9.0.0 release readiness

This is a candidate evidence ledger, not a release claim. The package workflow
creates an artifact only; it does not tag, publish, push, or create a GitHub
Release. A later explicit release action remains gated on recorded native-host
and downloaded-package evidence.

| Evidence | Status | Record |
| --- | --- | --- |
| Candidate revision | Verified candidate, not released | [Run 36107957765](https://github.com/thebpandey/agent-team/actions/runs/36107957765) passed at `59f45ee`; artifact `10851782034` includes the reviewed cutover correction. |
| Archive path whitelist and SHA-256 | Verified | The final downloaded artifact passed `sha256sum -c`; `agent-team-skill-9.0.0.zip` SHA-256 is `ddcb34d23d434596589035b867ada9b2980afd226d2780c78d406a98a26d30ef`. Packaged `CUTOVER.md` byte-matches `59f45ee`. |
| Linux CI Unix installer test | Verified | `ubuntu-latest` passed in run `36107957765`. |
| Windows CI native PowerShell installer test | Verified | `windows-latest` passed in run `36107957765`. This is installer evidence, not a native Codex/Claude host canary. |
| Beads 1.2.2 download, checksum, and fixture | Verified | The pinned release asset, disposable init, readonly status, and bounded ready command passed in run `36107957765`. |
| Downloaded archive checksum and Linux installation | Verified for installer bytes | The earlier downloaded archive `10851326442` installed identical skill trees into disposable Codex and Claude homes. Installer and skill bytes are unchanged at `59f45ee`; the final downloaded archive checksum was verified, but its install was not repeated. |
| Downloaded archive checksum and Windows installation | Unverified | No published/downloaded candidate has been checked. |
| Linux native Codex status canary | Verified for status only | The installed v9 skill was read in the active Codex session; `bd --readonly status --json` returned the existing 70-issue Beads summary without setup or dispatch. Native worker dispatch remains unverified. |
| Linux native Claude status canary | Verified for status only | Claude Code 2.1.282 ran `/agent-team status` with exit 0 and returned the same Beads summary. The response did not print the skill version; v9 use is inferred from its Beads-only behavior and the installed v9 tree, not asserted from the response alone. Native worker dispatch remains unverified. |
| Windows native Codex canary | Unverified | No Windows native-host canary is recorded. |
| Windows native Claude canary | Unverified | No Windows native-host canary is recorded. |
| Quiescent v8 cutover and rollback smoke | Live force-removal/install complete; no rollback claim | Disposable Linux dual-host v9 install/rollback preserved Git, Beads, and project files. The approved v8 managed uninstall failed its journal-budget preflight without changing managed files. The user then explicitly directed removal without a recovery journal or backup and deletion of prior v8 copies. The three exact v8 roots and private snapshot were removed; both installed v9 trees passed an independent five-file byte comparison. Pre-existing repository dirt prevents a claim that project files were byte-unchanged during cutover. |
| Optional Project Kickoff v9 bridge | Unverified | This is an interoperability report, not an Agent-Team standalone release gate. |

The standalone candidate must continue to work without Project Kickoff. Missing
Project Kickoff bridge evidence limits only that optional interoperability
claim; it must not be substituted for Beads, installer, archive-download, or
native-host evidence.

The v8 managed uninstaller covers every host in its manifest and discovers
default host homes even when explicit homes are supplied. The disposable smoke
therefore did not invoke it: doing so could inspect the live installation.
The live uninstall was attempted once after explicit approval and snapshot
review, but v8.0.15 reserves about 118 MB of journal preimages for 36 current
and historical entries against a 64 MiB cap. It rejected the operation before
changing managed files. There is no supported v8 backup-prune operation;
repeating uninstall or deleting backup files would not resolve the preflight.
After an explicit change of direction from the user, the three exact v8
installation roots and the private v8 cutover snapshot were deleted without a
new backup or a recovery-journal claim. The v9 candidate package was moved
out of that snapshot first. Both Linux host skill roots now contain the same
reviewed v9.0.0 files; the old shared v8 config and binary are absent. This
is not evidence of a successful managed v8 uninstall or a rollback test of
the destructive route. Native worker dispatch remains a separate release gate,
and Windows native-host execution is still unverified.
