# Agent-Team 8.0.13 release readiness

Version 8.0.13 is an **unpublished candidate**. It contains three focused fixes:

- accept observational compatibility fields from newer Beads output while retaining strict known dependency relation semantics;
- report stale or incompatible Project Kickoff handoffs for explicit resolution and record an intentional `--ignore-kickoff` setup decision; and
- omit Graphify code-only excluded media from both the source fingerprint and its byte accounting.

## Pending release evidence

The release workflow must still build the exact revision and complete its Linux, macOS, and Windows checks, package validation, archive and Windows-bundle canaries, rollback, provider verification, installed-update test, and revision-bound readiness gate. No publication, downloadable 8.0.13 assets, or post-publication installation result is claimed here.

Prospective assets are `agent-teamctl-8.0.13.zip`, `RELEASE.json`, `SBOM.cdx.json`, `SHA256SUMS`, and `agent-teamctl-8.0.13-windows-amd64.zip` with its `.sha256` sidecar. These names do not establish publication or availability. Until the workflow succeeds and the release is published, v8.0.12 remains the published package.
