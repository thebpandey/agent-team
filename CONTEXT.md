# Agent-Team implementation recovery index

Owner: root orchestrator. Updated: 2026-09-09 (local date).

## Current checkpoint — supersedes the historical state below

- Native capability continuation: user approved replacement access rooted at the canonical disposable Codex fixture. Fresh actual session **01a08648-1e51-7d70-8e56-95136422816d**, native UI **gpt-5.6-terra medium**, loaded applicable skills and ran the exact approved probe once from `.worktrees/capability-probe` after root submitted one-call Allow. Native result/final reported **9/9 functional checks passed**, including owned Playwright browser cleanup. Normal Ctrl-D exit0; linked worktree and integration remain clean. Terminal evidence: `harness-artifacts/native-dependencies/codex-worker-canonical-terminal.log`. Actual terminal usage: input38877, cached192768, output2824, reasoning1286; this is not matched benchmark evidence. Nested-root launch remains unverified; canonical-root access with linked command cwd is the observed successful scope.
- Before binding setup readiness, root must inspect the complete native result (terminal abbreviated it). Exact session JSONL read was denied outside repository root; no reroute or configuration change. User approval requested for the prepared `harness-artifacts/native-dependencies/summarize-codex-worker.mjs`, reading only that task-owned session and extracting controls, instruction-call paths, probe result and usage. Syntax check passed; **script has not run**. Setup version2 remains honestly incomplete. The fixture-root and probe approvals are satisfied and must not be requested again.

- Latest checkpoint: integration **2dacfd45e37ce69c6cb8d6bc878e5063ee7a4c96**, clean, supersedes the62688f4 details below. Native dependency preparation was approved and ran once in actual owner session01a084ad-8643-7331-95f0-00e5e28033a0; setup version2 is honestly incomplete. Serena's version includes the fixture Git suffix, and the npm LeanCTX shim lacked its binary.2dacfd4 fixes scoped Serena version parsing and installs checksum-pinned LeanCTX3.10.1 plus the verified complete skill without npm lifecycle scripts. Old npm shim/custom paths preserved; actual scoped Serena MCP lookup and LeanCTX isolated functional read passed. Independent review accepted; focused27/27 and final **469/469, zero failures/skips**,28484.037007ms passed with all real opt-ins (archive42deab573ca179aa). Stored native setup receipts remain unmodified version2 until a fresh owned preparation operation binds actual worker observations.
- Exact2dacfd4 package check passed; release-candidate-c and release-candidate-d each independently passed artifact validation and have identical SHA256 **493dec8bfe8bfefbc425439702f4216e4dd15d34d26a6ad0d74b729865f9acd8**. Both disposable installed hosts refreshed through the managed installer with backups/no conflicts. Earlier465/2060e7e archives below are historical.
- Fresh native Codex developer capability check observed requested/actual UI gpt-5.6-terra medium in codex/.worktrees/capability-probe. LeanCTX denied canonical fixture skill reads as outside that nested worktree root; no denied read was rerouted. The approved probe command reached one-call native approval, but the outer terminal expired at120s before input arrived; **probe did not run**, and no capability-probe process remained. Log codex-worker-probe-terminal.log preserves this failure. Replacement worker with canonical disposable fixture access root and the same assigned worktree is awaiting the user's specific scoped access approval; no global permission/config changes. The exact probe-worker.mjs execution is already approved and must not be re-requested.
- Root LeanCTX doctor freshly reconfirmed canonical repository root/cwd and40/40 checks. No main integration, push, tag, release or deployment. Full native lifecycle, native observation binding, active-task/packet recovery and matched repeated benchmark remain open. Native dependency preparation is no longer pending; older text below records the previous checkpoint.

- LeanCTX MCP `lean-ctx doctor` verified root/cwd `/home/server/dev/skills/agent-team`. Normal scoped reads of integration CLI, setup/initializer source/tests and HTML succeed. Independent reviewer also confirmed access. An out-of-root installed skill read through the CLI still reported its older `/tmp` boundary; it was not reset or used to reroute denied repository source. Keep using MCP `ctx_*` with explicit project paths/cwd.
- Resumed all original WIP without discarding changes. Integration HEAD **62688f4fe3bf01bb430839040e06a2bf3ba94eaa**, branch `codex/approved-decisions`, `.worktrees/integration`, clean. Prior2060e7e wires initializer/setup helpers, ships manifest/protocols, preserves guide changes and expands actual source/archive consumers. 82099a2 adds the native observation binding recipe; independent `/root/native_binding_review` accepted after actual child session/worktree/operation and skill-only path matching. Docs13/13, package and diff checks passed. Both disposable hosts updated to82099a2 through managed installer with backups/no conflicts. Native Claude read-only consumption prompted two wording clarifications in62688f4, independently accepted. Refresh scoped fixture docs before exercising the latest recipe. Full465/archive evidence below belongs to2060e7e; executable code remains unchanged.
- Independent review found two P2 gaps: explicit nonzero initial setup version was ignored; dependencies inspection ignored selected scope. Root reproduced 0/2 RED, repaired under the existing locking/API contracts, and passed affected82/82. Independent re-review59/59 accepted both; final 12-case matrix test diff accepted. Reviewer `/root/qualification_review` is the reusable review worker.
- Final current suite: **465/465 passed, zero failures/skips**, 27653ms. Command: `AGENT_TEAM_REAL_DEPS=1 AGENT_TEAM_REAL_LEAN_CTX=/home/server/.local/bin/lean-ctx AGENT_TEAM_REAL_BD=/home/server/.local/bin/bd AGENT_TEAM_REAL_SERENA=1 AGENT_TEAM_TEST_PINNED_PLAYWRIGHT_PACKAGE=1 AGENT_TEAM_BD=/home/server/.local/bin/bd AGENT_TEAM_QUALIFICATION_OUTPUT=/home/server/dev/skills/agent-team/harness-artifacts/native-dependencies node --test tests/hooks-*.test.mjs tests/qualification-beads.test.mjs`. Full suite includes all real opt-ins and now-authorized stale-lock child. Archive `450a0578dea7c02e` holds direct tool output.
- Source/ZIP × codex/claude-code/both × user/project now exercises **12 actual Node CLI lifecycle combinations**. A temporary test-harness failure came from global afterEach deleting shared archives after new subtests; replaced subtests with diagnostics and reran successfully. This was a test-only repair, not a product failure or waived assertion.
- Package validation passed. Built exact2060e7e twice into `harness-artifacts/release-candidate-{a,b}`. Each archive independently passed check-artifacts; identical SHA256 **13973a80fed7ade8f4748f0b31033a018dfa8b7165d9044e4c8dcfcb330490b4**. Check each archive separately; validator correctly rejects supplying two distributions at once.
- User explicitly allowed `codex`, `claude`, `playwright-cli` before resumption, approved the isolated `node -e` stale-lock fixture, and approved native trust for only the disposable Codex project/five candidate hooks. Those former denials are superseded for these exact scopes. No broad allowlist/config bypass is authorized.
- Actual Playwright CLI0.1.19 installation/probe/complete scoped companion/open-click-eval-close passed using production runner. A subsequent CLI list returned `(no browsers)`. Receipt/script and retained fixture are in `harness-artifacts/native-dependencies`; no global browser installation. Real selected dependency suite11/11 independently includes Serena MCP symbol lookup, LeanCTX isolated read, Beads routing/concurrent export, and pinned selected skills/detectors.
- Native Codex0.153.4 and Claude Code2.1.266 freshly loaded the project-installed7.0.0 skill in disposable `harness-artifacts/native-dependencies/host-fixtures/{codex,claude-code}`. Claude actual stream model `claude-fable-5-1`; SessionStart/PreToolUse/PostToolBatch observed. Codex native UI showed `gpt-6-astra high`; project and five exact hooks were trusted via native review, then SessionStart/PreToolUse observed. Native transport observations do not automatically populate a trusted/supported health claim.
- User approved the exact native health call; native `on-request` showed its MCP prompt and root selected one-call Allow. It completed once; archive expansion did not repeat execution. Installed/registered true, trust unknown and exercised false are correctly distinct from observed native trust. Session `01a084ad-8643-7331-95f0-00e5e28033a0` then actually compacted through `/compact`, with packaged PreCompact observed. `codex exec resume` restored the exact health command, once-only execution, limitations and no-publication fact without tools (archive93558f2c439b7e94). This checks health-context restoration only; active-task/pending-operation and packet-only recovery remain open.
- Disposable Codex standalone initialization succeeded: user continue accepted the exact pending call; native same-session resume/on-request one-call Allow returned applied/canonicalReady:true/ready:false for NATIVE-1. Setup version1 and TEAMS owner match actual session01a084ad-8643-7331-95f0-00e5e28033a0. TUI exited normally with Ctrl-D. Log codex-initialize-terminal.log; fixture baseline ef4f738 has one passing trim test and deliberately incomplete normalizer for native development/review qualification.
- **Pending individual approval:** exact native `node .agents/skills/agent-team/hooks/agent-team-cli.mjs dependencies-prepare --project /home/server/dev/skills/agent-team/harness-artifacts/native-dependencies/host-fixtures/codex --host codex --scope project --request /home/server/dev/skills/agent-team/harness-artifacts/native-dependencies/host-fixtures/codex/preparation.json`. Prepared request binds actual owner/setup version1/operation native-codex-prepare-1 with existing defaults and no optionals. Not yet run. Do not mistake earlier health/init approvals for this call's native approval. Read-only Claude consuming-host recipe check is separate.
- Claude recipe-consumption session06a3c9e5-d9f6-4f5f-b305-7caf05c7ea97 completed with actual claude-fable-5-1, no permission denials or child launches. Correctly rejected mismatched session/worktree and stale preparation replay, kept model unknowns and did not invent execution. It confused discovered-file inventory with complete reads of every skill and lacked clarity on router/dispatch data;62688f4 clarifies these. Private receipt claude-binding-consumer.json/archive9e2faa81031083b2. This is partial instruction qualification, not settings/dependency execution or a benchmark. Native TUI and Claude read-only process both exited; no known task-owned native process remains.
- Native interactive transport needs a real terminal: direct wrapped PTY produced TERM/non-TTY failure. Authorized `script -q -f -c 'codex ...' <private-log>` exposed the native UI; inspect log through ctx_read and send input through write_stdin. LeanCTX's outer native command ended after120s. No matching task-owned Codex TUI process remained when checked. Log is `harness-artifacts/native-dependencies/codex-trust-terminal.log`. No bypass flag, trust-file fabrication, or disabled LeanCTX was used.
- Root's native discovery/check receipts and observed usage are in `harness-artifacts/native-dependencies/resumed-qualification.json`. These are not a matched benchmark. Full native developer/reviewer lifecycle, all three entry paths, relevant fresh-worker Serena/Playwright access, actual compaction and packet-only recovery, and repeated matched baseline/candidate metrics remain required/unverified.
- Coverage review found the plain CLI has no native catalog or worker observations; exported runCommand accepts them through context. Committed82099a2 explains a small owned driver transferring inspected results with scope/path/version/actual child session/worktree/operation matching, preserving production probes and fresh version/new operation IDs. Real native settings/dependency handoff execution remains required. D01 needs no additional Node Context7 client: selected native results can enter its existing functional adapter; Context7 remains unselected. Optional bv preparation/licensing evidence remains conditional; do not invent eligibility.
- Remote main rechecked **6feb76a0a2df42fcbbd42c5bc3df0d36396bee0e**. No merge to main, push, tag, release, or cloud deployment. Required native qualification still gates publication. Preserve all older worktrees, user files and private evidence.

## Historical checkpoint (2026-09-08)

The access/command denials and pending CLI wiring described below are historical; use the current checkpoint above for resumption. Earlier incident disclosures remain part of the record.

## Authority and original sources

User authorized implementing finalized F01–F17 and D01–D20, testing, updating README and the original standalone HTML guide, committing, pushing origin/main and GitHub distribution **after successful qualification**. No push/tag/release/cloud deployment has occurred. Root TASKS.md is the sole tracker; this file is a recovery index, not another backlog.

- Specification: AGENT_TEAM_UX_AND_EFFICIENCY_AUDIT.md, sections 9–13; earlier sections are historical.
- Plan: docs/superpowers/plans/2026-09-08-agent-team-approved-decisions.md.
- Keep Beads. bv is optional referenced third-party software by Jeffrey Emanuel, with the full upstream LICENSE and restriction rider; no vendoring, white-labelling, relicensing or blanket eligibility claim.
- Preserve user documents and unrelated work. Ordinary findings are automatically repaired; no nested teams or new cloud deployment.

## Exact working state

- Main: 4a7d1c3 before this checkpoint, ahead of origin/main only by local plan/audit/guide/tracker checkpoints. Remote main last verified at 6feb76a0a2df42fcbbd42c5bc3df0d36396bee0e. Implementation is not merged to main.
- Integration: .worktrees/integration, branch codex/approved-decisions, HEAD **56335ce0dae9008f88195209f65775474cd3ecf2**. Root is sole editor/integrator.
- Accepted/integrated: AT01 through322bf89; AT02639a1fb; AT03d312db4; AT049ed9b115; AT05be4575e; workflowCLI05b67c5; graph5b61072; initializer945d235. Root hook deadline repairs acb7fb3 and snapshot producer e8e6d7a accepted. Version7.0.0 is explicitly unpublished, committed with README/changelog/scenarios/historical-diagram notice in3b5cfa4.
- Setup candidate **4d58e80c9a284348af85eae18dd20302b32ca102** merged at6fc820e. Independent scoped entrypoint review found no defects and ran17/17 tests; test bodies/supporting source were access-denied and not rerouted, so that review limitation remains. Previously accepted initializer internals are unchanged.
- Root integration WIP: hooks/agent-team-cli.mjs project-initialize wrapper; manifest initializer/setup-cli entries; README, GETTING_STARTED.md and HTML refinements; setup/hooks reference protocols; new tests/hooks-project-journey.test.mjs; expanded installed-consumer tests/hooks-artifacts.test.mjs; isolated tests/qualification-beads.test.mjs. Preserve all edits. **Setup commands still need importing, command flags and routing in the CLI.**

## Work and ownership

- .worktrees/setup-cli, codex/setup-cli-integration, clean4d58e80; author installation_implementation and reviewer lifecycle_review completed. Original fixes cover selected host/scope receipts, canonical run/capacity, locked owner reload, original tracker binding, legitimate Beads outage and project replacement during lock wait.
- .worktrees/playwright-companion, codex/playwright-companion, clean **7a44d82a5376cfc1967e4e9e67fa09e66d48a189**. Independent readiness_implementation review accepted the final cleanup repair50/50 without access limits; merged at56335ce. All workers are completed/idle; no repair remains active.
- Companion removes upstream workspace initialization; copies/verifies complete owned selected-scope companion on install/reuse, preserves custom files and scopes browser acquisition. Accepted: bounds128entries/depth12/1MiBfile/8MiBtree/64KiBmetadata; shared budget/signal on npm/functional children; no late receipt; guarded atomic rename; reserved time for exact-session cleanup before original deadline, including explicit cancellation. Insufficient reserve prevents opening. Persistent-session fake tests distinguish close_command_passed from unverified in caller-visible evidence. No kill-all or native cleanup certification.
- .worktrees/initialization945d235, .worktrees/installation5b61072, .worktrees/readiness9ed9b115 and .worktrees/dashboardbe4575e are accepted/clean/idle. .worktrees/runtimed312db4 has an older worker CONTEXT edit to preserve.
- Four concurrency slots; reuse completed agents for bounded reviews. send_message does not restart a completed agent; use followup_task for work.

## Verification

- Integration56335ce + WIP manifest: dependencies + companion + settings + setup + initializer + package **131/131**, zero skips. Exact command: `AGENT_TEAM_TEST_PINNED_PLAYWRIGHT_PACKAGE=1 node --test tests/hooks-playwright-companion.test.mjs tests/hooks-dependencies.test.mjs tests/hooks-settings.test.mjs tests/hooks-setup-cli.test.mjs tests/hooks-initialization.test.mjs tests/hooks-package.test.mjs`. Opt-in only downloads/verifies/copies the integrity-pinned package; browser/installer commands are labelled fakes, not native execution.
- Previous integration400e78e + WIP permitted wide: **415 tests:404 passed,2 failed,9 skipped**. Both failures are missing settings CLI routing (actual project journey and extracted consumer). Nine skipped tests need explicit real-dependency fixture selection. Do not call this a passing suite.
- Wide command: `node --test --test-skip-pattern='a dead stale-lock recovery claimant is reclaimed without displacing a new owner' tests/hooks-*.test.mjs`. Keep the forbidden node-e fixture excluded.
- Setup4d58e80: author17/17 and named61/61; independent entrypoint17/17. Initializer945d235 independently59/59. Companion7203e7a review accepted copy/commit guards; c2bf15a review accepted child cancellation; 7a44d82 final review accepted reserved exact-session cleanup50/50. Static pinned launcher/core inspection confirms CLI version0.1.19, not core1.63.0-alpha, without running it.
- Actual selected bd1.2.2 re-run1/1 passed using production routing isolation: same-owner claim, conflicting claim, linked-worktree read and fresh export. Not external-server multi-writer proof.
- Current HTML guide passed actual isolated Chromium at1440×1000,375×812,812×375: expanded sections/copy/keyboard/no external loads. Root inspected source/render; independent HTML-source review remains access-limited.
- Integrated static/mobile dashboard and loopback refresh/graph keyboard/filter browser checks passed. Graph is a labelled fixture, not real bv. Exact live fixturePID3103968 stopped; no known live helper.
- Earlier docs/package/archive38/38 predates expanded installed-consumer setup matrix; earlier permitted wide299/299 predates current changes. Neither certifies current final integration.
- Private evidence: harness-artifacts/integration-qualification.md, at-01..05 reports/reviews, guide-browser, at-05-browser and native-dependencies. Evidence is not another tracker.

## Access and publication gates — do not bypass

Native codex, claude, actual playwright-cli, node -e, review-package, UI skill search and with_server.py commands are permanently denied in this session. Do not retry via wrappers, alternate executables/API paths, allowlist changes or LEAN_CTX_DISABLED. Fresh worker discovery, model enforcement, native hooks/trust/lifecycle and mandatory actual browser-CLI checks remain unverified and block publication. Python Playwright browser evidence is not a substitute.

A pre-isolation LeanCTX probe changed the shared active-project boundary to /tmp. Root's normal source read of **integration/hooks/agent-team-cli.mjs is now denied**. Do not reroute that file read via Git, another tool or another agent, or change global config/allowlists. Other scoped paths may still be ordinarily accessible; stop on any denial. Correct workspace context must be restored before blocked CLI integration can continue. Test execution and permitted manifest/tracker updates remain available. Root rg of generated tee test log was also denied; do not reroute that log read. A subsequent permitted test run produced direct raw output and the counts above.

An earlier Serena probe may have touched default ~/.serena state. These incidents were disclosed; no global reset/deletion. Production now isolates XDG/SERENA_HOME and strips inherited Beads/Git routing. Earlier non-isolated probes and worker broad suites that accidentally ran node-e are not permitted qualification evidence.

No actual bv installation/execution or universal license eligibility is certified. gh auth was healthy for thebpandey; latest published baseline v6.5.0. No new global host/MCP installs are authorized by isolated tests.

## Resume sequence

1. Restore legitimate workspace source access before touching the denied CLI. Inspect it and wire setupCommandFlags/runSetupCommand; initializer envelope actor binding is already implemented. Do not fabricate native model/worker facts or retry denied commands in this session.
2. Run real Node project journey and expanded extracted/installed six host/scope combinations (both-host cases exercise each CLI). Fix consumer findings, then final permitted wide suite/package checks. Native hosts are separate gates.
3. Recheck affected guides/browser only as needed; retain original green standalone HTML with Project Kickoff first, local-preview warning and historical6.x diagrams notice.
4. Save tracker/evidence and commit completed local work. Do not push/tag/release or claim completion until required native qualification runs legitimately and passes.

## Skills already read

Main fully read applicable Agent-Team references, LeanCTX, plan/worktree/SDD and prompts, TDD, verification, writing-skills and testing reference, OpenAI docs, UIUXProMax and both references, webapp-testing and example, and ProjectKickoff skill/setup reference. Do not reread solely because of compaction. Skills do not waive access restrictions or authorize another tracker. Read finishing-branch skill when genuinely finishing. Maintainer-only browser environment is .superpowers/browser-venv with matching .superpowers/browser-binaries, not a product Python prerequisite.
